<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.chenhan.serialAgent.infrastructure.dao.UserDao">
    <select id="getUser" resultType="org.chenhan.serialAgent.infrastructure.po.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="getUserList" resultType="org.chenhan.serialAgent.infrastructure.po.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

</mapper>
